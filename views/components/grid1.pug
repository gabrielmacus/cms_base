script(src="/js/equalize.min.js")
script.
    $(document).ready(function () {

        $("#item-description").equalize("height");


    });

mixin addGrid(items)
 .w3-row-padding
  if items
   each item in items
    .w3-col.s12.m6.l4.w3-margin-top
          .w3-card-4
              img.img(src=item.pic.url)
              .w3-container.w3-center.second-color
                 p#item-description !{item.descripcion}
